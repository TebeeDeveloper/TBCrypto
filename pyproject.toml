[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "TBCrypto"
version = "0.1.0"
description = "My private project"
requires-python = ">=3.8"